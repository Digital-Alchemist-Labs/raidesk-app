# RAiDesk - 의료기기 인허가 어시스턴트

RAiDesk는 의료기기 인허가 과정을 안내하는 AI 어시스턴트 애플리케이션입니다. Next.js 15, TypeScript, Tailwind CSS v4를 기반으로 구축되었으며, 모던하고 글래스morphic한 디자인을 특징으로 합니다.

## 🎯 주요 기능

### 1. 대화형 인허가 가이드
- ChatGPT 스타일의 직관적인 대화 인터페이스
- 단계별 질문과 응답을 통한 인허가 프로세스 안내
- 실시간 진행 상황 표시

### 2. 지능형 분류 및 분석
- 제품 개념 입력을 통한 의료기기 해당 여부 자동 판단
- 품목 분류 및 등급 결정
- 사용 목적과 작용 원리 정의

### 3. 4가지 인허가 전략 플랜
- **최단 경로**: 가장 빠른 시장 진입 (6개월)
- **표준 경로**: 업계 표준을 따르는 균형잡힌 전략 (9-10개월)
- **보수적 경로**: 최대한의 근거 확보 (12-15개월)
- **혁신 경로**: 혁신의료기기 지정을 통한 우선심사 (7-8개월)

### 4. 상세 플랜 관리
- 공통규격 및 기본규격(성능평가) 타임라인
- 각 단계별 소요 기간, 비용, 위험도 분석
- 장단점 및 권장사항 제공
- 플랜 수정 및 커스터마이징 기능

### 5. 시각화 및 추적
- 실시간 요약 패널
- 의사결정 플로우차트 자동 생성 (Mermaid.js)
- 진행 단계 시각화

### 6. 데이터 관리
- 로컬 스토리지 자동 저장
- SQL 데이터베이스 통합 준비
- 세션 관리 및 복원

## 🚀 시작하기

### 필수 요구사항

- Node.js 20 이상
- npm 또는 yarn

### 설치

```bash
# 저장소 클론
git clone https://github.com/Digital-Alchemist-Labs/raidesk-app.git
cd raidesk-app

# 의존성 설치
npm install
```

### 환경 설정

`.env.local` 파일에서 다음 환경 변수를 설정하세요:

```env
# API 엔드포인트 (Python backend)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Mock 모드 활성화 (테스트용)
NEXT_PUBLIC_USE_MOCK=true

# 스토리지 타입 (localStorage 또는 sql)
NEXT_PUBLIC_STORAGE_TYPE=localStorage
```

### 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

### 프로덕션 빌드

```bash
npm run build
npm run start
```

## 📁 프로젝트 구조

```
raidesk-app/
├── app/                          # Next.js App Router
│   ├── layout.tsx               # 루트 레이아웃
│   ├── page.tsx                 # 메인 페이지
│   └── globals.css              # 글로벌 스타일 (Tailwind v4)
├── components/
│   ├── chat/                    # 채팅 인터페이스
│   │   ├── ChatInterface.tsx   # 메인 채팅 컨테이너
│   │   ├── MessageList.tsx     # 메시지 목록
│   │   └── InputArea.tsx       # 입력 영역
│   ├── panels/                  # 사이드 패널
│   │   ├── ProgressBar.tsx     # 진행 상황 바
│   │   ├── SummaryPanel.tsx    # 요약 패널
│   │   └── FlowchartPanel.tsx  # 플로우차트 패널
│   ├── plans/                   # 플랜 관련
│   │   ├── PlanLayout.tsx      # 플랜 레이아웃
│   │   ├── PlanCard.tsx        # 플랜 카드
│   │   └── PlanDetail.tsx      # 플랜 상세
│   └── ui/                      # 재사용 가능한 UI
│       ├── Button.tsx
│       ├── Card.tsx
│       ├── Input.tsx
│       └── Tabs.tsx
├── lib/
│   ├── api/                     # API 클라이언트
│   │   ├── client.ts           # API 클라이언트
│   │   ├── endpoints.ts        # 엔드포인트 정의
│   │   └── index.ts
│   ├── storage/                 # 스토리지 레이어
│   │   ├── localStorage.ts     # 로컬 스토리지 구현
│   │   ├── sqlAdapter.ts       # SQL 어댑터 (준비)
│   │   └── index.ts
│   └── mock/                    # 테스트 데이터
│       └── dummyData.ts
├── store/
│   └── useAppStore.ts          # Zustand 전역 상태
├── types/
│   └── index.ts                # TypeScript 타입 정의
└── README.md
```

## 🎨 기술 스택

- **프레임워크**: Next.js 15 (App Router)
- **언어**: TypeScript
- **스타일링**: Tailwind CSS v4
- **애니메이션**: Framer Motion
- **상태 관리**: Zustand
- **API 클라이언트**: Axios
- **아이콘**: Lucide React
- **차트/다이어그램**: Mermaid.js
- **데이터 관리**: React Query

## 🔧 주요 기능 설명

### Mock 모드

테스트 및 개발을 위해 Mock 모드를 사용할 수 있습니다:

```typescript
// .env.local
NEXT_PUBLIC_USE_MOCK=true
```

Mock 모드에서는 실제 백엔드 없이도 모든 기능을 테스트할 수 있습니다.

### 백엔드 통합

Python langchain/langgraph 백엔드와 통합하려면:

1. `.env.local`에서 `NEXT_PUBLIC_USE_MOCK=false` 설정
2. `NEXT_PUBLIC_API_URL`을 백엔드 주소로 설정
3. 백엔드가 다음 엔드포인트를 제공해야 합니다:
   - `POST /api/classify` - 의료기기 분류
   - `POST /api/purpose` - 사용목적/원리 생성
   - `POST /api/standards` - 플랜 생성
   - `POST /api/refine` - 플랜 수정

### SQL 데이터베이스 통합

SQL 데이터베이스를 사용하려면:

1. `lib/storage/sqlAdapter.ts`에서 SQL 구현 완성
2. Prisma 또는 Drizzle ORM 설치 및 설정
3. `.env.local`에서 `NEXT_PUBLIC_STORAGE_TYPE=sql` 설정

## 🎯 사용 방법

### 1. 시작
- 애플리케이션을 열면 환영 메시지가 표시됩니다
- 의료기기 아이디어를 입력하세요

### 2. 분류 확인
- AI가 제품이 의료기기인지 자동으로 판단합니다
- 품목 분류 및 등급을 확인하고 수정할 수 있습니다

### 3. 사용 목적 정의
- AI가 제안하는 사용 목적과 작용 원리를 검토합니다
- 필요시 수정을 요청할 수 있습니다

### 4. 플랜 선택
- 4가지 인허가 전략 중 하나를 선택합니다
- 각 플랜의 상세 정보를 확인할 수 있습니다
- 플랜을 수정하거나 커스터마이징할 수 있습니다

### 5. 요약 및 플로우차트
- 우측 패널에서 진행 상황 요약을 확인합니다
- 플로우차트 탭에서 의사결정 과정을 시각화합니다

## 🔒 보안 및 프라이버시

- 기본적으로 모든 데이터는 브라우저의 localStorage에만 저장됩니다
- 네트워크 통신은 백엔드 API와만 이루어집니다
- 민감정보 처리시 적절한 암호화를 사용하세요

## 🤝 기여

이슈 및 풀 리퀘스트를 환영합니다!

## 📝 라이선스

ISC License

## 📧 문의

프로젝트 관련 문의사항은 GitHub 이슈를 통해 남겨주세요.

---

**Made with ❤️ for better medical device regulatory processes**
